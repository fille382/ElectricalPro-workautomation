# PocketBase server URL — baked into the build
# When serving from PocketBase directly, use relative URL (empty = same origin)
# When developing locally, set to http://127.0.0.1:8090
VITE_POCKETBASE_URL=https://api.electricalpro.se
